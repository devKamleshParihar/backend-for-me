function getData() {
    // Get the input element by its ID
    var inputData = document.getElementById("inputData");

    // Get the value entered by the user
    var enteredData = inputData.value;

    // Display the entered data (you can do whatever you want with the data)
    alert("Entered Data: " + enteredData);
}
